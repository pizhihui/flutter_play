import 'package:flutter/material.dart';


class airplayScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("这是airplay"),),
      body: Center(child: Text("这是airplay的正文内容"),),
    );
  }
}


