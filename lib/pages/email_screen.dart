import 'package:flutter/material.dart';


class EmailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("这是邮件"),),
      body: Center(child: Text("这是邮件的正文内容"),),
    );
  }
}


