import 'package:flutter/material.dart';


class PagesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("这是pages"),),
      body: Center(child: Text("这是pages的正文内容"),),
    );
  }
}


